sudo: required

addons:
  apt:
    packages:
      - docker-ce

services:
  - docker

before_script:
  - docker build -t cryptocard .

script:
  - docker run cryptocard gradle check --info --debug
  - docker run cryptocard gradle test --info --debug

notifications:
  email: false